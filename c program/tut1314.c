#include <stdio.h>
#include <math.h>
int main()
{
    // code of do  while loop
    //  loop are use for save time ,code reusabilithy,traversing
    /* syntax
    loop start- check the condition - if false the condition the exit the loop
    if condition is true then the the loop will execute, and we also need the termination condition
    if loop in not terminated the the loop goes into infinite satate
     types of loop while loop, do while loop, for loop.
     */
    /*
       int i= 1;
        do{
            i=i+1;
            printf("%d\n");
        }while(i<=10);
    */
 /*  int num, index=0;
   printf("enter a number\n");
   scanf("%d",&num);
   do
   {
     printf("%d\n",index);
     index=index+1;
   }while(index<num);
   */

  //new code
/*  int x=4,y,z;
  y=--x;
  z=x--;
  printf("%d,%d,%d\n",x,y,z);
  return 0;
  */


 // new code
 int i;
 while(i=10)
 {
  printf("%d\n",i);
  i=i+1;
 }
 return 0;
}
