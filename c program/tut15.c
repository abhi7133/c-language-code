#include <stdio.h>
#include <conio.h>
#include <math.h>
int main()
{
    // code for do for loop
    // code with harry
    //  all three condition in the for loop is optional
    // the expression one is optional,and it expression of initalization of variable
    // the expression two is main condition which decide that when the loop is terminated
    // the expression three is incrementation
    int i, j;
    for (i = 0, j = 0; i < 5; i++)
    {
        printf("%d %d\n", i,j);
    }
    return 0;
}
